spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/first_db
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
#      hbm2ddl:
#        auto: create-drop


jwt:
  secret: javainuse
  expiration: 3600
